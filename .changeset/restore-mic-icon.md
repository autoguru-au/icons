---
'@autoguru/icons': minor
---

Restore the `mic` icon (`MicIcon`). It was dropped in the 2.0 Phosphor migration (AG-19073), which left the FMO booking AI Summary "Powered by" footer rendering a literal microphone instead of the brand mark. This re-adds the custom `mic.svg` so `MicIcon` is exported again.

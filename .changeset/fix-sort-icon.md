---
'@autoguru/icons': patch
---

Fix `sort` icon rendering — replace stroke-based artwork with filled chevron paths from the Fleet Client Portal design, and tighten the viewBox to remove excess whitespace around the glyph.
